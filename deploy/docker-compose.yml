services:
  levellens:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    env_file:
      - .env.example
    ports:
      - "8000:8000"
    volumes:
      - levellens-data:/app/data
    restart: unless-stopped

volumes:
  levellens-data:

